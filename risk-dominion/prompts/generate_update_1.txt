You are in the project root of Risk: Dominion. You are implementing a complete UI/UX overhaul of the frontend. This is the final change before the demo.

Read every file listed below before generating any output. Do not skip any file. Understand the full component tree, current styling approach, interaction patterns, and SpacetimeDB hook wiring before making changes.

---

FILES TO READ FIRST

Frontend source:
- app/client/index.html
- app/client/tailwind.config.js
- app/client/package.json
- app/client/src/App.tsx
- app/client/src/constants.ts
- app/client/src/types.ts
- app/client/src/components/Map.tsx
- app/client/src/components/Territory.tsx
- app/client/src/components/CardHand.tsx
- app/client/src/components/ActionCard.tsx
- app/client/src/components/IntelPanel.tsx
- app/client/src/components/VictoryScreen.tsx
- app/client/src/components/EventTicker.tsx
- app/client/src/components/StrategistAlerts.tsx
- app/client/src/components/ActionBar.tsx
- app/client/src/components/QueryBar.tsx
- app/client/src/components/ResultsPanel.tsx
- app/client/src/components/ChatPanel.tsx
- app/client/src/components/PlayerIndicator.tsx
- app/client/src/components/ReplayControls.tsx
- app/client/src/components/SpectatorOverlay.tsx
- app/client/src/hooks/useSubscriptions.ts
- app/client/src/utils/territoryHelpers.ts

Spec documents:
- update-1/MASTERPLAN_UPDATE_1.md
- update-1/IMPLEMENTATION_STRATEGY_UPDATE_1.md
- AESTHETIC.md
- UIUX.md
- UX_OVERHAUL_DECISIONS.md

---

WHAT THIS OVERHAUL IS

A visual and interaction overhaul. Game mechanics are unchanged. Every reducer, every AI system, every database operation, every subscription — all unchanged. What changes is how the game looks, feels, and responds to the player.

The core changes:
1. Swap Cinzel font to Rajdhani everywhere (all display labels, UI text, card labels, continent banners, overlay text).
2. Remove all emoji characters from all files.
3. Add five new files: TitleScreen.tsx, ColorLegend.tsx, CommandBar.tsx, ChatWindow.tsx, soundEngine.ts.
4. Restyle IntelPanel.tsx, VictoryScreen.tsx, EventTicker.tsx, StrategistAlerts.tsx to the warm parchment palette.
5. Add VictoryScreen shockwave ring animation and sound.
6. Add heatmap overlay rendering to the map for query results.
7. Wire CommandBar into App.tsx with Enter key trigger.

---

WHAT NOT TO CHANGE

Do not change any of the following:
- Hex grid layout in Map.tsx — the map is three continent columns of hex tiles. Keep all SVG geometry, world silhouette background, and column structure.
- Territory.tsx hex tile SVG — keep all polygon coordinates, quadrant fills, center medallion, border, glow logic, and droppable behavior.
- CardHand.tsx fanned layout — keep the fan angle math, the yLift calculation, the float-up animation, the margin overlap between cards, and the relative positioning container.
- DnD context and drag handlers in App.tsx — keep DndContext, onDragStart, onDragEnd, getValidMilitaryTargets call, and all reducer calls.
- Any reducer call — militaryAttack, economicInvest, deployAgent, dismissStrategistAlert, sendChatMessage, startGame.
- useSubscriptions hook wiring.
- QueryBar.tsx, ResultsPanel.tsx, ChatPanel.tsx, PlayerIndicator.tsx, ReplayControls.tsx, SpectatorOverlay.tsx — do NOT delete these files. Do NOT remove their import statements from App.tsx.
- Game constants — do NOT change TOTAL_TERRITORIES, WIN_UNIFIED_TERRITORIES, ACTION_REGEN_SECONDS, ADJACENCY, TERRITORY_NAMES, or PLAYER_COLORS.
- Mode logic — player/spectator/replay mode switching stays.
- Spectator overlay rendering.
- Replay controls rendering.

---

GENERATION ORDER

Generate files in this exact order. Every file must only depend on types and utilities that were generated earlier in this list.

1.  app/client/index.html (MODIFIED)
2.  app/client/tailwind.config.js (MODIFIED)
3.  app/client/package.json (MODIFIED)
4.  app/client/src/constants.ts (MODIFIED)
5.  app/client/src/types.ts (MODIFIED)
6.  app/client/src/utils/soundEngine.ts (NEW)
7.  app/client/src/components/ColorLegend.tsx (NEW)
8.  app/client/src/components/TitleScreen.tsx (NEW)
9.  app/client/src/components/ActionCard.tsx (MODIFIED)
10. app/client/src/components/ChatWindow.tsx (NEW)
11. app/client/src/components/CommandBar.tsx (NEW)
12. app/client/src/components/Territory.tsx (MODIFIED)
13. app/client/src/components/Map.tsx (MODIFIED)
14. app/client/src/components/CardHand.tsx (MODIFIED)
15. app/client/src/components/IntelPanel.tsx (MODIFIED)
16. app/client/src/components/VictoryScreen.tsx (MODIFIED)
17. app/client/src/components/EventTicker.tsx (MODIFIED)
18. app/client/src/components/StrategistAlerts.tsx (MODIFIED)
19. app/client/src/App.tsx (MODIFIED)

Mark each output file as MODIFIED or NEW at the top (as a comment). Do not output any files marked DELETED.

---

RULES

Font:
- Replace every instance of `fontFamily: "Cinzel, serif"` with `fontFamily: "Rajdhani, sans-serif"` in every component file.
- In index.html, update the Google Fonts link to include Rajdhani with weights 500, 600, 700. Remove Cinzel from the link.
- In tailwind.config.js, update the font-display token value to 'Rajdhani'.
- Keep Orbitron for digital chrome (clock readouts, numeric overlays).
- Keep JetBrains Mono for all numbers, data values, timestamps, query input, chat messages.
- After the overhaul, `grep -r "Cinzel" app/client/src` must return zero results.

Emoji:
- Remove every emoji character from every file you touch.
- In Map.tsx: delete the CONTINENT_ICONS constant and the <span> that rendered emoji next to the continent name. Render the continent name text only.
- In App.tsx: the loading screen has a sword emoji (⚔) — replace it with a plain SVG glyph or remove it entirely.
- After the overhaul, no emoji character may appear anywhere in app/client/src.

Colors:
- Use the warm parchment palette throughout:
  - bg-deep: #0d0a06
  - bg-mid: #1a1610
  - bg-surface: #221e18
  - gold: #d4a017
  - gold-dim: #9a8870
  - border-subtle: #3d3525
  - neutral-hex: #2a2318
  - Player colors from PLAYER_COLORS in constants.ts.
- Do not introduce ocean blue, cool gray, or any color outside this palette.

No D3:
- Do not add d3 to package.json.
- Do not write any D3 import.
- All map rendering stays as SVG hex grid with React.
- Query heatmap: shade individual Territory hex tiles by passing a heatmap intensity value (0.0–1.0) as a prop. Territory.tsx applies a semi-transparent colored overlay on the hex face proportional to the intensity. No D3 required.

SpacetimeDB 2.4.1:
- Use useTable, useProcedure, useReducer, DbConnection.builder() only.
- Do not use deprecated patterns: useSpacetimeTable, useSpacetimeReducer, or any direct SDK calls that bypass these hooks.

New component specifications:

soundEngine.ts:
- Export: `playSoundEvent(type: SoundEvent): void`
- SoundEvent type: 'card-play' | 'territory-flip' | 'cultural-pressure' | 'victory' | 'defeat'
- AudioContext created once, lazily on first call, stored in module scope.
- Frequencies: card-play 800Hz/50ms, territory-flip 120Hz/150ms, cultural-pressure 200Hz–400Hz swept/200ms, victory C5(523Hz)-E5(659Hz)-G5(784Hz) sequence 100ms each, defeat G4(392Hz)-C4(262Hz) 150ms each.
- All synthesis via OscillatorNode + GainNode. No audio files.

ColorLegend.tsx:
- Position: fixed, bottom-left, z-index above map but below modals.
- Four rows: player 1 "You" (PLAYER_COLORS[1]), then AI_PLAYERS[0], [1], [2] with their names and colors.
- Each row: a 12x12px colored square + player name in Rajdhani 12px.
- Warm parchment background, border-subtle border, 60% opacity at rest.

TitleScreen.tsx:
- Props: onDone: () => void
- Renders a fixed full-screen overlay (bg-deep at 0.85 opacity).
- Centers "RISK: DOMINION" text: Rajdhani 700, 48px, gold color.
- Animation: fade in 300ms, hold 2000ms, fade out 500ms.
- Calls onDone() after 2800ms total. Parent unmounts it on onDone.
- The hex map renders normally behind this overlay (it is not blocking the map from mounting).

ChatWindow.tsx:
- Props: targetPlayerId: number, messages: ChatMessage[], onSend: (text: string) => void, onClose: () => void
- Position: fixed, bottom: 16px, right: 16px, width: 280px, height: 320px.
- Header: Rajdhani 600, player name, close button (x).
- Message list: scrollable, JetBrains Mono 11px. Player messages align right, AI messages align left.
- Input field at bottom: JetBrains Mono, dark background, gold border on focus.
- AI responses must be 100 characters or fewer. Enforce this in the AI prompt string within the component or in the chat pipeline.

CommandBar.tsx:
- Props: onClose: () => void, onChatOpen: (playerId: number) => void, onQuery: (text: string) => void
- Position: renders in the document flow at the top of the main content area, OR fixed top center (60% width, z-index above map).
- Layout: dark surface (bg-surface), gold `>` prompt on left, JetBrains Mono text input.
- On `>` click: shows dropdown panel with four sections: INTEL, CHAT, EVENTS, ADVICE. Each section has a header and 2–4 selectable options.
- On Enter with input text: parse command. "chat with {name}" (case-insensitive) triggers onChatOpen with the matching player id from AI_PLAYERS. Any unrecognized text triggers onQuery(text). Unrecognized command with no match: shake animation on the bar (CSS transform translateX keyframe).
- On Escape: call onClose().

In App.tsx, make these specific changes (do not change anything else):
- Import and render TitleScreen conditionally. Add state: `const [showTitle, setShowTitle] = useState(true)`. Render `{showTitle && <TitleScreen onDone={() => setShowTitle(false)} />}` inside the root div before all other content.
- Import and render ColorLegend inside the map area div or as a fixed overlay near the Map component.
- Import and render CommandBar. Add state: `const [commandBarOpen, setCommandBarOpen] = useState(false)`. Add state: `const [chatWindowTarget, setChatWindowTarget] = useState<number | null>(null)`. In the onKey handler, add a case for `Enter` key (when not in an input) that sets `setCommandBarOpen(true)`. Render `{commandBarOpen && <CommandBar onClose={() => setCommandBarOpen(false)} onChatOpen={(id) => { setChatWindowTarget(id); setCommandBarOpen(false); }} onQuery={(text) => { /* pass to query pipeline */ }} />}`.
- Import and render ChatWindow. Render `{chatWindowTarget !== null && <ChatWindow targetPlayerId={chatWindowTarget} messages={...} onSend={...} onClose={() => setChatWindowTarget(null)} />}`.
- Remove the sword emoji (⚔) from the isReady=false loading screen. Replace with a Rajdhani text span or an SVG.
- Replace the `fontFamily: "Cinzel, serif"` string in the loading screen span with Rajdhani.
- Add heatmap intensity props plumbing: if queryHighlights is non-empty, compute a per-territory intensity map and pass it to Map.tsx. Map.tsx passes individual intensity values to each Territory.tsx tile.

---

SUCCESS CRITERIA

After applying all modifications, all of the following must be true:

1. `npm run build` compiles without errors.
2. App loads. TitleScreen appears in Rajdhani gold. Fades after ~2.8s. Map is visible behind it.
3. Hex map renders. Continent banners show Rajdhani text only. Zero emoji anywhere.
4. Territory hex tiles render correctly: quadrant fills, medallion troop count, Rajdhani name label below.
5. Fanned card hand visible at bottom in player mode. Cards have Rajdhani labels (ATTACK, INVEST, DEPLOY).
6. Military card drag highlights valid adjacent enemy targets. Economic/Covert drag highlights all 12 territories.
7. Card drop calls the correct reducer. Territory state updates reflect the action.
8. Enter opens CommandBar. Clicking `>` shows dropdown with INTEL, CHAT, EVENTS, ADVICE sections. Escape closes.
9. "chat with Zhao" command opens ChatWindow bottom-right. AI responds in 100 characters or fewer.
10. Query result shades hex tiles as heatmap. Caption visible. Auto-dismisses after 10 seconds.
11. ColorLegend visible bottom-left. Four entries with correct player colors and Rajdhani names.
12. VictoryScreen shows shockwave ring animation on win. playSoundEvent('victory') fires.
13. Five sound events trigger at correct moments: card-play, territory-flip, cultural-pressure, victory, defeat.
14. grep -r "Cinzel" app/client/src returns zero results.
15. No emoji characters remain in any file in app/client/src.
16. PlayerIndicator.tsx, ReplayControls.tsx, SpectatorOverlay.tsx, QueryBar.tsx, ResultsPanel.tsx, ChatPanel.tsx all still exist and are imported in App.tsx exactly as before.

This is the final generation. Output every changed file. Mark each MODIFIED or NEW at the top. Generate now.
