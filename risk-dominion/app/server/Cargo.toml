[package]
name = "risk_dominion"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
# `unstable` enables procedures + ctx.http (Slice 2+: the only way to call Claude).
spacetimedb = { version = "2.4.1", features = ["unstable"] }
log = "0.4"
serde_json = "1"
